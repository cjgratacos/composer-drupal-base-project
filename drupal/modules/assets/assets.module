<?php

/**
 * @file
 * Contains assets.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function assets_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the assets module.
    case 'help.page.assets':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('My Awesome Module') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme().
 */
function assets_theme() {
  return [
    'assets' => [
      'render element' => 'children',
    ],
  ];
}
